#!/bin/sh

# 如果没有选中文件，直接退出
[ -z "$NNN_SEL" ] && exit 1

# 压缩包名字
ARCHIVE="archive.tar.gz"

# 切到当前目录（nnn 会传进来）
cd "$PWD" || exit 1

# 使用 tar 压缩选中的文件
tar -czf "$ARCHIVE" -T "$NNN_SEL"

# 可选：提示
notify-send "nnn" "Created $ARCHIVE" 2>/dev/null
